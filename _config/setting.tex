\renewcommand{\thefootnote}{[\arabic{footnote}]}
\setlength{\skip\footins}{20pt plus 4pt minus 2pt}
\setlength{\footnotesep}{10pt}
\interfootnotelinepenalty=10000

\makeatletter
\renewcommand\@makefntext[1]{%
    \noindent\hspace*{1.5em}%
    \makebox[1.3em][l]{\@makefnmark}%
    #1
}
\makeatother

% ======================================================
% 标题 & title
% ======================================================

\usepackage{titlesec}

% section 颜色
\titleformat{\section}[block]
  {\normalfont\Large\bfseries\color{xmured}\filcenter}
  {\thesection}
  {1em}
  {}

% subsection 颜色 + 居左
\titleformat{\subsection}[block]
  {\normalfont\large\bfseries\filright}
  {\thesubsection}
  {1em}
  {}

\DeclareCaptionLabelSeparator{fullwidthcolon}{：}
\captionsetup{labelsep=fullwidthcolon}

% ======================================================
% 日志 & warning
% ======================================================

% 忽略一定范围内已知的 overfull/underfull 盒子警告，避免编辑时 Problems 面板噪声过多。
\hfuzz=120pt
\vfuzz=30pt
\hbadness=10000
\vbadness=10000
\overfullrule=0pt

% ======================================================
% 列表 & lists
% ======================================================

\setlist[itemize]{
    label=\raisebox{0.2ex}{\scriptsize$\bullet$},
    labelsep=0.7em,
    topsep=0.5ex plus 0.2ex minus 0.1ex,
    itemsep=0.35ex plus 0.1ex minus 0.1ex,
    parsep=0pt,
    partopsep=0pt
}

% ======================================================
% 数学 & math
% ======================================================

\setlength{\jot}{6pt}

\AtBeginDocument{%
    \setlength{\abovedisplayskip}{8pt plus 2pt minus 2pt}%
    \setlength{\belowdisplayskip}{8pt plus 2pt minus 2pt}%
    \setlength{\abovedisplayshortskip}{6pt plus 2pt minus 2pt}%
    \setlength{\belowdisplayshortskip}{6pt plus 2pt minus 2pt}%
}

% `setstretch` 会把 array 类数学环境的 strut 一起放大，
% 导致 matrix / cases 第一行上方和最后一行下方出现额外留白。
\newcommand{\ResetMathArraySpacing}{%
    \renewcommand{\arraystretch}{1}%
}

\AtBeginEnvironment{matrix}{\ResetMathArraySpacing}
\AtBeginEnvironment{smallmatrix}{\ResetMathArraySpacing}
\AtBeginEnvironment{cases}{\ResetMathArraySpacing}
\AtBeginEnvironment{dcases}{\ResetMathArraySpacing}
\AtBeginEnvironment{dcases*}{\ResetMathArraySpacing}
\AtBeginEnvironment{rcases}{\ResetMathArraySpacing}
\AtBeginEnvironment{rcases*}{\ResetMathArraySpacing}
\AtBeginEnvironment{drcases}{\ResetMathArraySpacing}
\AtBeginEnvironment{drcases*}{\ResetMathArraySpacing}
